# --- Security ---
ADMIN_USERNAME=admin
# Password formats:
# - Plain text: {noop}your-password
# - BCrypt: {bcrypt}$2a$10$hash
ADMIN_PASSWORD={noop}your-secure-password

# Optional: Custom key for Remember Me (auto-generated if not set)
# REMEMBER_ME_KEY=some-very-long-random-string-here-min-32-chars

# HTTPS settings (set to true when using reverse proxy with SSL/TLS)
HTTPS_ONLY=false

# Cookie SameSite policy (lax, strict, none)
# COOKIE_SAME_SITE=lax

# --- Database ---
DB_PASSWORD=changeme

# --- Camera ---
CAMERA_IP=192.168.0.2
CAMERA_PORT=80
CAMERA_USERNAME=admin
CAMERA_PASSWORD=password
CAMERA_RTSP_PORT=554
# Timezone identifier: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TIMEZONE=UTC

# --- Optional ---
# BACKUP_BASE_DIR=/backups
# RECORDING_DOWNLOAD_DIR=/tmp/recordings
# STREAM_TEMP_DIR=/tmp/stream
# MAIN_TRACK=101
# SUB_TRACK=102
# APP_FRONTEND_URL=http://localhost:8081
